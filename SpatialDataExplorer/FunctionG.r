#Funkcja G # potrzebna paczka install.packages("inlabru")?
#Funkcja G
#longitude <- file2[["Geodata.DecLongitude"]]
#latitude <- file2[["Geodata.DecLatitude"]]
#lonlat <- cbind(longitude, latitude)
#library(sp)
#pts <- SpatialPoints(lonlat)
#spatial.to.ppp(p, pts)
#Gest(p, r=NULL, breaks=NULL, correction=c("rs", "km", "han"), domain=NULL)